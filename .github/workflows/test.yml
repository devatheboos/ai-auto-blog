name: ✅ Confirm Steps Visible

on:
  workflow_dispatch:

jobs:
  show-steps:
    name: Debug Step View
    runs-on: ubuntu-latest

    steps:
      - name: Step 1 - Say Hello
        run: echo "👋 Hello from GitHub Actions"

      - name: Step 2 - Say Goodbye
        run: echo "👋 Goodbye!"
