name: ✅ Auto Blog Post Generator

on:
  workflow_dispatch:
  schedule:
    - cron: "*/30 * * * *"

jobs:
  show-steps:
    name: Generate AI Blog
    runs-on: ubuntu-latest

    env:
      OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
      GIT_EMAIL: ${{ secrets.GIT_EMAIL }}
      GIT_NAME: ${{ secrets.GIT_NAME }}
      REPO_PUSH_URL: ${{ secrets.REPO_PUSH_URL }}

    steps:
      - name: Step 1 - Checkout Repo
        uses: actions/checkout@v3

      - name: Step 2 - Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: 18

      - name: Step 3 - Install Dependencies
        run: npm install

      - name: Step 4 - Run Blog Generator
        run: |
          echo "Running fetchAndGenerate.mjs..."
          node scripts/fetchAndGenerate.mjs || {
            echo "❌ Generation failed"
            exit 1
          }

      - name: Step 5 - Show Files Created
        run: |
          echo "📂 content/posts/"
          ls -lh content/posts || echo "⚠️ No posts found"
          echo "🖼 content/images/"
          ls -lh content/images || echo "⚠️ No images found"

            - name: Step 6 - Git Commit & Push
        run: |
          git config --global user.email "$GIT_EMAIL"
          git config --global user.name "$GIT_NAME"

          ls content/posts/*.md 2>/dev/null && git add content/posts/*.md || echo "⚠️ No markdown files"
          ls content/images/*.png 2>/dev/null && git add content/images/*.png || echo "⚠️ No image files"
          ls logs/*.log 2>/dev/null && git add logs/*.log || echo "⚠️ No log files"

          if git diff --cached --quiet; then
            echo "🟡 No changes to commit"
          else
            git commit -m "🤖 New AI post: $(date)"
            git remote set-url origin "$REPO_PUSH_URL"
            git push origin HEAD:main || {
              echo "❌ Git push failed"
              exit 1
            }
          fi
