name: Auto Blog Post Generator

on:
  workflow_dispatch:
  schedule:
    - cron: "*/30 * * * *"

jobs:
  build:
    name: Generate Blog
    runs-on: ubuntu-latest

    steps:
      - name: Echo Hello
        run: echo "🎉 Hello from the Auto Blog Generator"
